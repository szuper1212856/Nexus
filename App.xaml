<Application x:Class="NEXUS.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NEXUS.ViewModels"
             xmlns:views="clr-namespace:NEXUS.Views"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Order matters: later dictionaries reference resources from earlier ones. -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Theme.xaml" />
                <ResourceDictionary Source="Resources/Converters.xaml" />
                <ResourceDictionary Source="Resources/Styles.xaml" />
                <ResourceDictionary Source="Resources/TaskCard.xaml" />
                <ResourceDictionary Source="Resources/Ops.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Operations subsystem views -->
            <DataTemplate DataType="{x:Type vm:CommandCenterViewModel}">
                <views:CommandCenterView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:DevicesViewModel}">
                <views:DevicesView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:NetworkViewModel}">
                <views:NetworkView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ServersViewModel}">
                <views:ServersView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SecurityViewModel}">
                <views:SecurityView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:UsersViewModel}">
                <views:UsersView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:AccessViewModel}">
                <views:AccessView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:AutomationsViewModel}">
                <views:AutomationsView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:TerminalViewModel}">
                <views:TerminalView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:LogsViewModel}">
                <views:LogsView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SystemViewModel}">
                <views:SystemView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ServiceDeskViewModel}">
                <views:ServiceDeskView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:DirectoryViewModel}">
                <views:DirectoryView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:FilesViewModel}">
                <views:FilesView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:DocumentsViewModel}">
                <views:DocumentsView />
            </DataTemplate>

            <!-- Task subsystem views -->
            <DataTemplate DataType="{x:Type vm:OverviewViewModel}">
                <views:OverviewView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:TasksViewModel}">
                <views:TasksView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:QuickActionsViewModel}">
                <views:QuickActionsView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:FocusViewModel}">
                <views:FocusView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:AnalyticsViewModel}">
                <views:AnalyticsView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ActivityViewModel}">
                <views:ActivityView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <views:SettingsView />
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
